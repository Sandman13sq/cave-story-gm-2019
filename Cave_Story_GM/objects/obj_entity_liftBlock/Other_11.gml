/// @desc 

// Inherit the parent event
event_inherited();

switch(state)
{
	case(10):
		var _dir = dirAngle(entityDirection);
		//x_speed = lengthdir_x(1, _dir);
		//y_speed = lengthdir_y(1, _dir);
		break;
}