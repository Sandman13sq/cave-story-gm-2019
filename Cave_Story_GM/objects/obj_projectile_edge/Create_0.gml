/// @desc 

// Inherit the parent event
event_inherited();

flag = flag | 1 << projFlag.breakBreakable;
flag = flag | 1 << projFlag.destroyOnHit;
// Alarm for Trail
alarm[0] = 1;

radius = sprite_get_width(sprite_index) / 2;
width = 3;

color = Col.onyxPink;
colorPos = 0;
colorSpeed = 0.1;