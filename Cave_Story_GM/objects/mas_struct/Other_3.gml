/// @desc 

if global.listCount > 0
{
	/*
	var _l = ds_list_size(global.listTracker);
	
	for (var i = 0; i < _l; i++)
	{
		list_free(global.listTracker[| 0]);
	}
	
	*/
	
	//msg(global.listCount, "lists left over...");
}

// list_free(global.listTracker);