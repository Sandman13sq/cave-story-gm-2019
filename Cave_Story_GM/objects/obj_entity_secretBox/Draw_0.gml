/// @desc 

// Inherit the parent event
//event_inherited();

draw_shape_rect(
	bbox_left, bbox_top, bbox_right + 1, bbox_bottom + 1,
	color, alpha
	);
	
/*
draw_shape_rect(
	x + x_left, x + y_up, 
	y + x_right, y + y_down,
	c_caveBlack, alpha
	);