/// @desc Runs code associated with flags
function run_flags() {

	/*
	if healthPoints == 0 && healthPointsMax > 0
	{
		if flag & 1 << Entity_Flag.deathEvent {runEvent(event)}
		else
		{
			instance_destroy();
		}
	}

/* end run_flags */
}
